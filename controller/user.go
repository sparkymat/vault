package controller

import (
	"net/http"

	"github.com/sparkymat/vault/settings"
)

func UserRegistrationHandler(config settings.ApplicationConfig, response http.ResponseWriter, request *http.Request) {
}
